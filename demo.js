// example of usage for external library with : npm install lodash
// installing npm install -g nodemon / nodemon is a tool that helps develop Node.js based applications by automatically restarting the node application when file changes in the directory are detected.

var _ = require('lodash')

console.log(_.random(1,100))

